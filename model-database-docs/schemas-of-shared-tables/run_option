CREATE TABLE run_option ( run_id       INT            NOT NULL,  option_key   VARCHAR(255)   NOT NULL,  option_value VARCHAR(32000) NOT NULL,  PRIMARY KEY (run_id, option_key), CONSTRAINT run_option_mk FOREIGN KEY (run_id) REFERENCES run_lst(run_id) )
